
To calculate the efficiency and its error, we use signal MC events, run through the \textsc{BaBar} GEANT4 simulation with all the cuts applied. We record the number of reconstructed events passing the cuts/skims ($n_{\text{rec}}$) and the number of events that we started with ($n$):
